<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base TV theme — uses AppCompat parent to avoid Leanback internal resource issues -->
    <style name="Theme.ownTV" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="colorPrimary">#FF2196F3</item>
        <item name="colorPrimaryDark">#FF1976D2</item>
        <item name="colorAccent">#FF2196F3</item>
    </style>

    <!-- Playback theme (full screen, no decorations) -->
    <style name="Theme.ownTV.Playback" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
